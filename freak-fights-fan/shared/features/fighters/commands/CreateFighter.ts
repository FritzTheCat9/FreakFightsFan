export namespace CreateFighter {
    export interface Command {
        firstName: string;
        lastName: string;
        nickname: string;
        instagramUrl: string;
        imageBase64: string;
    }
}
