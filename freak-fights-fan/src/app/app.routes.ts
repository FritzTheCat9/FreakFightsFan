import { Routes } from '@angular/router';
import { DictionariesComponent } from './dictionaries/dictionaries.component';

export const routes: Routes = [
    { path: 'app-dictionaries', component: DictionariesComponent },
];
