@implements IDialogContentComponent<UpdateMyDictionaryItem.Command>

@inject IMyDictionaryItemApiClient MyDictionaryItemApiClient
@inject IExceptionHandler ExceptionHandler

<EditForm Model="Content" OnValidSubmit="HandleValidSubmit">
    <CustomValidator @ref="CustomValidator" />
    <FluentValidationValidator />

    <FluentDialogBody>
        <XTextField @bind-Value="Content.Name" For="(() => Content.Name)" Label="Name" />
        <FluentValidationMessage For="(() => Content.Name)" />

        <XTextField @bind-Value="Content.Code" For="(() => Content.Code)" Label="Code" />
        <FluentValidationMessage For="(() => Content.Code)" />
    </FluentDialogBody>

    <FluentDialogFooter>
        <FluentButton Type="Fluent.ButtonType.Submit" Color="Fluent.Color.Info">Update</FluentButton>
    </FluentDialogFooter>
</EditForm>

@code {
    [CascadingParameter] FluentDialog Dialog { get; set; }
    [Parameter] public UpdateMyDictionaryItem.Command Content { get; set; } = new();

    private CustomValidator CustomValidator;

    private async Task HandleValidSubmit()
    {
        try
        {
            await MyDictionaryItemApiClient.UpdateMyDictionaryItem(Content);
            await Dialog.CloseAsync(Fluent.DialogResult.Ok(true));
        }
        catch (MyValidationException validationException)
        {
            CustomValidator.DisplayErrors(validationException.Errors);
        }
        catch (Exception ex)
        {
            ExceptionHandler.HandleExceptions(ex);
        }
    }
}