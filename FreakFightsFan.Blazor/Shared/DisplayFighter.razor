@if (Fighter is not null)
{
    <MudStack Justify=Justify.Center
              Spacing=1>
        @if (!string.IsNullOrEmpty(Fighter.Image?.Url))
        {
            <MudLink Class="align-self-center"
                     Href=@Fighter.Image.Url
                     Target="_blank">
                <MudImage Alt=@Localizer[nameof(AppStrings.Image)]
                          Fluid=true
                          ObjectFit=ObjectFit.Contain
                          Src=@Fighter.Image.Url
                          Style="max-height: 250px;" />
            </MudLink>
        }
        else
        {
            <MudImage Alt=@Localizer[nameof(AppStrings.Image)]
                      Fluid=true
                      ObjectFit=ObjectFit.Contain
                      Src="images_default/default_user_image.png"
                      Style="max-height: 250px;" />
        }
        <MudText Class="align-self-center"
                 Style="font-weight: bold; text-align: center;"
                 Typo=Typo.subtitle1>
            @Fighter.Nickname.ToUpper()
        </MudText>
        <MudText Class="align-self-center"
                 Style="text-align: center;"
                 Typo=Typo.subtitle2>
            @Fighter.FirstName.ToUpper() @Fighter.LastName.ToUpper()
        </MudText>
    </MudStack>
}